\begin{table*}[!t]
    \centering
    \resizebox{\textwidth}{!}{
        \begin{tabular}{lcl}
        \textbf{Recency Class} & \textbf{Expected time until answer change} & \textbf{Example Question} \\
        \midrule
        An-Hour & Within an hour & What is the current stock price of Apple? \\
        A-Few-Hours & Within a few hours & What is the current traffic situation on the A9 highway? \\
        A-Day & Within a day & What is today’s weather forecast for Munich? \\
        A-Few-Days & Within a few days & What movies are currently trending on Netflix this week? \\
        A-Week & Within a week & What are the top-ranked songs on the Billboard chart this week? \\
        A-Few-Weeks & Within a few weeks & What is the current FIFA world ranking of the German national team? \\
        A-Month & Within a month & What is the current unemployment rate in Italy? \\
        A-Few-Months & Within a few months & What is the current inflation rate in the Eurozone? \\
        A-Year & Within a year & What is the current version of the Java programming language? \\
        A-Few-Years & Within a few years & Who is the president of the United States? \\
        Many-Years & After many years & What is the population of Germany? \\
        Never & Never changes & What is the chemical symbol for gold? \\
        \end{tabular}
    }
    \caption{The recency classes proposed by the \textit{RecencyQA} paper, along with example questions for each class.}
    \label{tab:recency-classes}
\end{table*}

Our dataset creation is closely aligned with the \textit{RecencyQA} paper's methodology, but we extend their approach by incorporating multi-event questions and refining the stationarity aspect. The dataset generation pipeline consists of two main stages: question generation and recency labeling, as illustrated in Figure~\ref{fig:pipeline}. This pipeline constructs (as described in the following sections) a diverse set of recency-aware questions, each annotated with appropriate recency labels and contextual conditions. The code and dataset are publicly available at GitHub\footnote{\href{https://github.com/Fabianstw/RecencyQA_MultiEvent}{GitHub | RecencyQA\_MultiEvent}} for reproducibility and further research.

\subsection{Question Taxonomy}
As a core component of the dataset generation pipeline, we organize questions along a small set of structural dimensions, which are stored as explicit metadata for every record.
\begin{itemize}
    \item \textbf{Stationarity}: Questions differ in whether their correct answers are stable over time or change as the underlying real-world situation evolves. Non-stationary questions require access to recent information, while stationary questions remain valid independently of time.
    \item \textbf{Multi-Event}: Questions may depend on a single situation or combine information from multiple distinct or connected events. Multi-event questions increase temporal and structural complexity by requiring the integration of information across several concurrent or related processes.
    \item \textbf{Inter-event relationship}: When multiple events are involved, their relationship can be causal—where one event influences another—or purely temporal, where events co-occur in time without an assumed dependency.
    \item \textbf{Recency classes}: Each question is associated with a recency class indicating the expected timescale on which its answer may change (e.g., hours, days, or years). We adopt the recency classes proposed by the \textit{RecencyQA} paper (see \autoref{tab:recency-classes}).
    \item \textbf{Contextual conditions}: Recency is not absolute but depends on the assumed state of the world. Contextual conditions describe the situation under which a given recency class applies, allowing the same question (under Non-Stationary) to have different recency labels.
\end{itemize}

\subsection{Question Selection}
Having defined the question taxonomy, the next step in the pipeline is to select an initial set of seed questions from which new items are generated. These seeds serve as prompts that anchor topic, style, and temporal structure while still allowing the language model to introduce new events, domains, and cross-event interactions.

In our setting, we randomly select \num{75} questions from the original \textit{RecencyQA} dataset as seed inputs. The selected questions are evenly distributed across stationarity and recency classes. All questions are provided to the model in a structured JSON format (see Appendix~\ref{app:sec:input-json}), which allows the generator to preserve key properties of the original question while flexibly expanding it into new single- and multi-event formulations.

\subsection{Question generation}
\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.7\textwidth]{res/PipelineWorkflow.png}
  \caption{Caption text.}
  \label{fig:pipeline}
\end{figure*}

Prompt templates instruct the model to output exactly two questions per call and forbid placeholders or paraphrasing of the seed (Appendix~\ref{app:gen-prompts}). A helper repeats calls until the target count is reached for each mode, reissuing only the missing portion to avoid over-generation. The ten prompt families are: stationary single (Appendix~\ref{prompt:stat-single}); stationary two-event causal (Appendix~\ref{prompt:stat-2-causal}) and temporal-only (Appendix~\ref{prompt:stat-2-temporal}); stationary three-event causal (Appendix~\ref{prompt:stat-3-causal}) and temporal-only (Appendix~\ref{prompt:stat-3-temporal}); non-stationary single (Appendix~\ref{prompt:nonstat-single}); non-stationary two-event causal (Appendix~\ref{prompt:nonstat-2-causal}) and temporal-only (Appendix~\ref{prompt:nonstat-2-temporal}); and non-stationary three-event causal (Appendix~\ref{prompt:nonstat-3-causal}) and temporal-only (Appendix~\ref{prompt:nonstat-3-temporal}). Every generated question is tagged with the seed identifier and metadata for stationarity, event dependency, number of events, and (when applicable) whether the multi-event relation was prompted as causal or temporal-only.

\subsection{Recency labeling}
Each question is labeled with recency classes that describe how quickly its answer should be refreshed. We use the following twelve labels:
\begin{center}
    \begin{tabular}{ll}
    An hour & A few hours \\
    A day & A few days \\
    A week & A few weeks \\
    A month & A few months \\
    A year & A few years \\
    Many years & Never
    \end{tabular}
\end{center}
Stationary questions receive one label; non-stationary questions receive two labels to capture alternative temporal regimes. For every label we also elicit a one-sentence temporal context (event, phase, or condition) that makes the question relevant. The labeling prompt prohibits meta-reasoning or explicit time stamps and is reproduced in Appendix~\ref{app:label-prompts} via the stationary template (Appendix~\ref{prompt:label-stat}) and the non-stationary template (Appendix~\ref{prompt:label-nonstat}). Labels and contexts are merged into a structured list so downstream models can pair each recency class with its motivating scenario. We retain only entries whose labels parse as JSON and whose context count matches the expected label count.

\subsection{Sampling and size}
Running the ten-generation-mode configuration on 75 seed questions yields 1{,}498 retained questions after discarding malformed generations that fail JSON parsing or lack labels. The ensure-and-top-off loop keeps modes balanced: 748 stationary vs. 750 non-stationary questions; 300 single-event, 598 two-event, and 600 three-event items; and within multi-event items 600 were prompted as causal chains and 598 as temporal-only pairs or triples. Mode totals differ from the ideal 1{,}500 by two items that were filtered for malformed JSON.

\subsection{Output format}
All examples are written as one JSON object per line. Each record stores the seed question identifier, question text, stationarity flag, event dependency type, number of events, the causal vs. temporal-only generation tag for multi-event items, and the list of recency labels with their paired contexts. The resulting corpus offers controlled variation across temporal behaviors and provides explicit supervision for estimating when an answer is likely to drift. We also keep the original seed linkage so downstream work can group generated questions by provenance or remove seed-specific leakage.

\subsection{Dataset Statistics}
The released split contains 1{,}498 examples. Stationarity is balanced (748 stationary, 750 non-stationary), and label cardinality follows the prompt contract (one label for stationary, two for non-stationary). Event cardinality is spread across 300 single-event, 598 two-event, and 600 three-event questions, with multi-event items split nearly evenly between causal (600) and temporal-only (598) prompting. The most common recency labels are ``A-Year'' (406), ``A-Day'' (368), and ``A-Few-Days'' (341); the tail includes a handful of model-emitted variants (e.g., ``A-Quarter'', ``A-Few-Minutes'') that remain in the data for transparency and can be remapped if a downstream system requires a closed label set.